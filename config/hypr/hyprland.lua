hl.config({ autogenerated = false })

hl.monitor({
	output = "DP-3",
	mode = "1920x1080@280",
	position = "1080x1080",
	scale = 1,
	transform = 0,
})

hl.monitor({
	output = "HDMI-A-1",
	mode = "1920x1080@60",
	position = "1080x0",
	scale = 1,
	transform = 0,
})

hl.monitor({
	output = "DP-1",
	mode = "1920x1080@180",
	position = "0x240",
	scale = 1,
	transform = 1,
})

hl.monitor({
	output = "DP-2",
	mode = "1920x1080@180",
	position = "3000x240",
	scale = 1,
	transform = 3,
})

hl.workspace_rule({ workspace = "1", monitor = "DP-3", default = true })
hl.workspace_rule({ workspace = "2", monitor = "HDMI-A-1", default = true })
hl.workspace_rule({ workspace = "3", monitor = "DP-1", default = true })
hl.workspace_rule({ workspace = "4", monitor = "DP-2", default = true })

hl.env("XCURSOR_SIZE", "24")
hl.env("HYPRCURSOR_SIZE", "24")
hl.env("LIBVA_DRIVER_NAME", "nvidia")
hl.env("__GLX_VENDOR_LIBRARY_NAME", "nvidia")
hl.env("ELECTRON_OZONE_PLATFORM_HINT", "auto")
hl.env("NVD_BACKEND", "direct")

hl.config({
	general = {
		gaps_in = 5,
		gaps_out = 5,
		border_size = 2,
		col = {
			active_border = "rgba(89b4faee)",
			inactive_border = "rgba(313244aa)",
		},
		resize_on_border = false,
		allow_tearing = true,
		layout = "dwindle",
	},
	decoration = {
		rounding = 0,
		active_opacity = 1.0,
		inactive_opacity = 1.0,
		dim_inactive = false,
		shadow = {
			enabled = false,
		},
		blur = {
			enabled = false,
		},
	},
	animations = {
		enabled = false,
	},
	render = {
		direct_scanout = false,
	},
	dwindle = {
		preserve_split = true,
	},
	master = {
		new_status = "master",
	},
	misc = {
		force_default_wallpaper = 0,
		disable_hyprland_logo = true,
		vrr = 0,
	},
	input = {
		kb_layout = "lv",
		follow_mouse = 1,
		sensitivity = 0,
		accel_profile = "flat",
		touchpad = {
			natural_scroll = false,
		},
	},
})

local mainMod = "SUPER"

hl.bind(mainMod .. " + Return", hl.dsp.exec_cmd("alacritty"))
hl.bind(mainMod .. " + space", hl.dsp.exec_cmd("rofi -show drun -normal-window"))
hl.bind(mainMod .. " + X", hl.dsp.exec_cmd("alacritty -e nvim"))
hl.bind(mainMod .. " + F", hl.dsp.exec_cmd("dolphin"))
hl.bind(mainMod .. " + B", hl.dsp.exec_cmd("librewolf"))
hl.bind(mainMod .. " + T", hl.dsp.exec_cmd("signal-desktop"))
hl.bind(mainMod .. " + O", hl.dsp.exec_cmd("obs"))
hl.bind(mainMod .. " + R", hl.dsp.exec_cmd("resolve"))
hl.bind(mainMod .. " + M", hl.dsp.exec_cmd("audacious"))
hl.bind(mainMod .. " + P", hl.dsp.exec_cmd("protonvpn-app"))
hl.bind(mainMod .. " + SHIFT + A", hl.dsp.exec_cmd("pavucontrol"))
hl.bind(mainMod .. " + SHIFT + T", hl.dsp.exec_cmd("alacritty -e btop"))
hl.bind(mainMod .. " + SHIFT + L", hl.dsp.exec_cmd("hyprlock"))

hl.bind(mainMod .. " + Escape", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/rofi-powermenu.sh'"))
hl.bind(mainMod .. " + SHIFT + space", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/sysmenu.sh'"))
hl.bind(mainMod .. " + N", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/rofi-notif.sh'"))
hl.bind(mainMod .. " + SHIFT + M", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/rofi-media.sh'"))
hl.bind(mainMod .. " + SHIFT + D", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/downloader.sh'"))
hl.bind(mainMod .. " + G", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/rofi-emoji.sh'"))
hl.bind(mainMod .. " + C", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/rofi-calendar.sh'"))
hl.bind(mainMod .. " + SHIFT + B", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/rofi-web.sh'"))
hl.bind(mainMod .. " + SHIFT + W", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/wallpaper-selector.sh'"))
hl.bind(mainMod .. " + SHIFT + I", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/idle-toggle.sh'"))
hl.bind(mainMod .. " + SHIFT + P", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/power_profile.sh'"))
hl.bind(mainMod .. " + SHIFT + N", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/dnd-toggle.sh'"))
hl.bind(mainMod .. " + SHIFT + E", hl.dsp.exec_cmd("sh -c '$HOME/.config/scripts/screen_search.sh'"))
hl.bind(
	mainMod .. " + SHIFT + U",
	hl.dsp.exec_cmd(
		"alacritty -e sh -c '$HOME/.config/scripts/system_update.sh; echo \"Press [Enter] to close...\"; read'"
	)
)
hl.bind(
	mainMod .. " + SHIFT + C",
	hl.dsp.exec_cmd(
		"alacritty -e sh -c '$HOME/.config/scripts/system_clean.sh; echo \"Press [Enter] to close...\"; read'"
	)
)

hl.bind(
	mainMod .. " + SHIFT + S",
	hl.dsp.exec_cmd(
		'sh -c \'mkdir -p $HOME/Pictures/Screenshots && f=$HOME/Pictures/Screenshots/scr_$(date +%s).png && grim -g "$(slurp)" "$f" && wl-copy < "$f"\''
	)
)
hl.bind(
	mainMod .. " + SHIFT + X",
	hl.dsp.exec_cmd(
		'sh -c \'mkdir -p $HOME/Pictures/Screenshots && f=$HOME/Pictures/Screenshots/scr_$(date +%s).png && grim "$f" && wl-copy < "$f"\''
	)
)
hl.bind(mainMod .. " + SHIFT + H", hl.dsp.exec_cmd("hyprpicker -a"))

hl.bind(mainMod .. " + Q", hl.dsp.window.close())
hl.bind(mainMod .. " + SHIFT + Q", hl.dsp.exit())
hl.bind(mainMod .. " + Z", hl.dsp.window.float({ action = "toggle" }))
hl.bind(mainMod .. " + W", hl.dsp.window.fullscreen())
hl.bind(mainMod .. " + Tab", hl.dsp.focus({ workspace = "previous" }))

hl.bind(mainMod .. " + D", hl.dsp.layout("togglesplit"))

hl.bind(mainMod .. " + left", hl.dsp.focus({ direction = "left" }))
hl.bind(mainMod .. " + right", hl.dsp.focus({ direction = "right" }))
hl.bind(mainMod .. " + up", hl.dsp.focus({ direction = "up" }))
hl.bind(mainMod .. " + down", hl.dsp.focus({ direction = "down" }))

for i = 1, 9 do
	hl.bind(mainMod .. " + " .. i, hl.dsp.focus({ workspace = i }))
	hl.bind(mainMod .. " + SHIFT + " .. i, hl.dsp.window.move({ workspace = i }))
end

hl.bind(mainMod .. " + mouse_down", hl.dsp.focus({ workspace = "e+1" }))
hl.bind(mainMod .. " + mouse_up", hl.dsp.focus({ workspace = "e-1" }))

hl.bind(mainMod .. " + mouse:272", hl.dsp.window.drag(), { mouse = true })
hl.bind(mainMod .. " + mouse:273", hl.dsp.window.resize(), { mouse = true })

hl.bind(
	"XF86AudioRaiseVolume",
	hl.dsp.exec_cmd("pactl set-sink-volume @DEFAULT_SINK@ +5%"),
	{ locked = true, repeating = true }
)
hl.bind(
	"XF86AudioLowerVolume",
	hl.dsp.exec_cmd("pactl set-sink-volume @DEFAULT_SINK@ -5%"),
	{ locked = true, repeating = true }
)
hl.bind("XF86AudioMute", hl.dsp.exec_cmd("pactl set-sink-mute @DEFAULT_SINK@ toggle"), { locked = true })
hl.bind("XF86MonBrightnessUp", hl.dsp.exec_cmd("brightnessctl set 5%+"), { locked = true, repeating = true })
hl.bind("XF86MonBrightnessDown", hl.dsp.exec_cmd("brightnessctl set 5%-"), { locked = true, repeating = true })
hl.bind("XF86AudioPlay", hl.dsp.exec_cmd("playerctl play-pause"), { locked = true })
hl.bind("XF86AudioNext", hl.dsp.exec_cmd("playerctl next"), { locked = true })
hl.bind("XF86AudioPrev", hl.dsp.exec_cmd("playerctl previous"), { locked = true })
